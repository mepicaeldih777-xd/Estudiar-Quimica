---
phase: 01-cimiento-de-datos-y-estructura
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env, src/lib/supabase.ts, database/schema.sql]
autonomous: true
requirements: [FR3, NFR1]
user_setup:
  - service: supabase
    why: "Almacenamiento de datos jerárquicos y adaptativos"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Project Settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Project Settings -> API"

must_haves:
  truths:
    - "El esquema SQL se puede ejecutar sin errores"
    - "El cliente de Supabase está configurado en el código"
  artifacts:
    - path: "database/schema.sql"
      provides: "Definición de tablas temas y actividades"
      contains: "CREATE TABLE temas"
    - path: "src/lib/supabase.ts"
      provides: "Cliente de conexión a base de datos"
---

<objective>
Configurar la base de datos (Supabase) y definir el esquema SQL para temas y actividades.

Purpose: Establecer el cimiento de persistencia para la jerarquía del conocimiento químico.
Output: Archivo de esquema SQL y cliente de conexión.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/DATABASE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear archivo de esquema SQL</name>
  <files>database/schema.sql</files>
  <action>Escribir el DDL para las tablas 'temas' y 'actividades' basado en DATABASE.md, incluyendo la relación recursiva parent_id.</action>
  <verify>Verificar que el SQL sea válido para PostgreSQL</verify>
  <done>Archivo database/schema.sql creado</done>
</task>

<task type="auto">
  <name>Task 2: Configurar cliente de Supabase</name>
  <files>src/lib/supabase.ts, .env.example</files>
  <action>Instalar @supabase/supabase-js y crear el cliente singleton. Crear .env.example con las variables necesarias.</action>
  <verify>npm list @supabase/supabase-js</verify>
  <done>Cliente configurado y dependencias instaladas</done>
</task>

</tasks>

<verification>
- [ ] tsc --noEmit (si se usa TS)
- [ ] database/schema.sql contiene CREATE TABLE temas con parent_id
</verification>

<success_criteria>
- Tablas definidas con integridad referencial.
- Infraestructura de conexión lista para seeding.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cimiento-de-datos-y-estructura/01-01-SUMMARY.md`
</output>
